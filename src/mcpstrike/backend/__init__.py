"""Optional standalone backend for local subprocess execution.

By default mcpstrike connects to an external HexStrike server. Install with
``pipx install ".[backend]"`` to get the ``mcpstrike-backend`` command as a
lightweight alternative that runs tools locally.
"""
